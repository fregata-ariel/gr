# tools/devenv/env.sh — machine-specific values for backup.sh (copy to ~/.config/gr-devenv/env.sh; git-ignored)
BACKUP_DEST=/mnt/data/backups/gr   # generations are written as <BACKUP_DEST>/<YYYYMMDD-HHMM>/
BACKUP_KEEP=14                     # generations kept by `backup.sh prune`
